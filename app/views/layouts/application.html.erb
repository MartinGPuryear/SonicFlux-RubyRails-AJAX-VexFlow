<!DOCTYPE html>
<html>
<head>
  <meta charset='UTF-8' />
  <meta http-equiv='X-UA-Compatible' content='IE=edge' />
  <meta name='description' content='Sonic Flux Rails page' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <meta name='author' content='Martin Puryear' />
  
  <%= stylesheet_link_tag    "application", media: "all" %>
  
  <!-- // <script type='text/javascript' src='http://localhost:6789/socket.io/socket.io.js'></script> -->
  <%= javascript_include_tag "application" %>


  <script type="text/javascript">

    // console.log('about to call io.connect');
    // var io = io.connect("http://localhost:6789"); 
    // var io = io.connect("http://localhost:6789", {force_connection: true} ); 
    // var io = io.connect("http://localhost:6789", {'force new connection': true} ); 
    // console.log('completed call to io.connect');

  </script>
  
  <%= csrf_meta_tags %>
  
  <title>SonicFlux</title>
</head>
<body>
	<div class='container-fluid'>
    <%= render "shared/navbar" %>

<%    if flash[:errors] != nil
      flash[:errors].each{ | key, value | value.each { | val |  %> 
          <p class='validation_error'><%= key %> <%= val %></p>
<%        }
      }
      end
      if flash[:alert] != nil  %> 
      <p class='alert'><%= flash[:alert] %></p>
<%    end   
      if flash[:notice] != nil  %> 
      <p class='notice'><%= flash[:notice] %></p>
<%    end   %>
    
		<%= yield %>
		
	</div>
</body>
</html>
