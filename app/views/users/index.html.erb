<h1>Users list</h1>
<h4>(admin-only)</h4>

<a href='<%= new_user_path %>'>New User</a>

<% 	if @users.length	%>
		<table class='table table-condensed table-striped table-hover'>
			<thead>
				<tr>
					<th>ID</th>
					<th>Player Tag</th>
					<th>Difficulty Level</th>
					<th>Progress</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
<%		@users.each { | user | %>
				<tr>
					<td><%= user.id %></td>
					<td><%= user.player_tag %></td>
					<td><%= user.difficulty_level.desc %></td>
					<td><a href='/progress/<%= user.id %>'>Progress</a></td>
					<td>
						<a href='<%= user_path(user.id) %>'>Show</a>
							| 
						<a href='<%= edit_user_path(user.id) %>'>Edit</a>
							| 
						<a href='<%= user_path(user.id) %>' data-method='delete'>Delete</a>
					</td>
				</tr>
<%		}	%>
			</tbody>
		</table>
<%	end  %>
