<h1>Question Type Detail</h1>
<h4>(admin-only)</h4>

<a href='<%= question_types_path %>'>All Question Types</a>
	| 
<a href='<%= new_question_type_path %>'>New Question Type</a>

<p>Detailed information about a single type of a question.</p>

<a href='<%= edit_question_type_path(@question_type.id) %>'>Edit</a>
	| 
<a href='<%= question_type_path(@question_type.id) %>' data-method='delete'>Delete</a>

<table class='table table-condensed table-striped table-hover'>
	<thead>
		<tr>
			<th>ID</th>
			<th>Prompt</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><%= @question_type.id %></td>
			<td><%= @question_type.prompt %></td>
		</tr>
	</tbody>
</table>
