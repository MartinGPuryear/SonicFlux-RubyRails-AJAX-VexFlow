<h1>Rounds List</h1>
<h4>(admin-only)</h4>

<a href='<%= new_round_path %>'>New Round</a>

<p>Unlikely to be used.  Would show the historical list of all rounds (num_participants, difficulty_level, created_at, updated_at), with linkage to its multiple Results and Question_Occurrences.  Could also delete from here (which would dependent-destroy the associated Results and Question_Occurrences).</p>

<% 	if @rounds.length	%>
		<table class='table table-condensed table-striped table-hover'>
			<thead>
				<tr>
					<th>ID</th>
					<th>Num Participants</th>
					<th>Difficulty Level</th>
					<th>Num Questions</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
<%		@rounds.each { | r | %>
				<tr>
					<td><%= r.id %></td>
					<td><%= r.num_participants %></td>
					<td><%= r.difficulty_level.desc %></td>
					<td><%= r.max_qo_index %></td>
					<td>
						<a href='<%= round_path(r.id) %>'>Show</a>
							| 
						<a href='<%= edit_round_path(r.id) %>'>Edit</a>
							| 
						<a href='<%= round_path(r.id) %>' data-method='delete'>Delete</a>
					</td>
				</tr>
<%		}	%>
			</tbody>
		</table>
<%	end  %>
