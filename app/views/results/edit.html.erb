<h1>Results Editor</h1>
<h4>(admin-only)</h4>

<a href='<%= results_path %>'>All Results</a>
	| 
<a href='<%= new_result_path %>'>New Result</a>

<p>This is where historical results can be adjusted.  Probably useful for testing the Leaderboards.</p>

<a href='<%= result_path(@result.id) %>'>Show</a>
	| 
<a href='<%= result_path(@result.id) %>' data-method='delete'>Delete</a>

<%= simple_form_for(@result, defaults: { input_html: { class: 'form-control' } }, html: {class: 'form-vertical' }) do | r |	%>
    <%= r.input :user_id, input_html: {autofocus: true }        %>
    <%= r.input :round_id                                       %>
    <%= r.input :round_complete                                 %>
    <%= r.input :num_correct                                    %>
    <%= r.input :num_skipped                                    %>
    <%= r.input :num_incorrect                                  %>
    <%= r.input :points                                         %>
    <%= r.input :rank                                           %>
    <%= r.submit 'Update', class: 'btn btn-primary form-button' %>
<% end %>
