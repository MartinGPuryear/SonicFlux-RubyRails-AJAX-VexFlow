<h1>Question Occurrences</h1>
<h4>(admin-only)</h4>

<a href='<%= question_occurrences_path %>'>All Question Occurrences</a>
	| 
<a href='<%= new_question_occurrence_path %>'>New Question Occurrence</a>

<p>Admins change the historical record for a question in a round.</p>

<a href='<%= question_occurrence_path(@question_occurrence.id) %>'>Show</a>
	| 
<a href='<%= question_occurrence_path(@question_occurrence.id) %>' data-method='delete'>Delete</a>

<%= simple_form_for(@question_occurrence, defaults: { input_html: { class: 'form-control' } }, html: {class: 'form-vertical' }) do | qo |	%>
    <%= qo.input :round_id, input_html: {autofocus: true }		 %>
    <%= qo.input :question_id									 %>
    <%= qo.input :index_in_round								 %>
    <%= qo.submit 'Update', class: 'btn btn-primary form-button' %>
<% end %>
