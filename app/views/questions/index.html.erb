<h1>Questions List</h1>
<h4>(admin-only)</h4>

<a href='<%= new_question_path %>'>New Question</a>

<p>This is where an Admin sees the list of all questions, sortable and filterable by Question Type.  Perhaps also includes statistics such as how frequently offered, how frequently skipped or incorrect, etc.</p>

<% 	if @questions.length	%>
		<table class='table table-condensed table-striped table-hover'>
			<thead>
				<tr>
					<th>ID</th>
					<th>Question Type ID</th>
					<th>Choice Type</th>
					<th>Difficulty Level</th>
					<th>Content</th>
					<th>Correct Choice ID</th>
					<th>Close Choice ID</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
<%		@questions.each { | q | %>
				<tr>
					<td><%= q.id %></td>
					<td><%= q.question_type_id %></td>
					<td><%= q.choice_type %></td>
					<td><%= q.difficulty_level.desc %></td>
					<td><%= q.content %></td>
					<td><%= q.correct_choice_id %></td>
					<td><%= q.close_choice_id %></td>
					<td>
						<a href='<%= question_path(q.id) %>'>Show</a>
							| 
						<a href='<%= edit_question_path(q.id) %>'>Edit</a>
							| 
						<a href='<%= question_path(q.id) %>' data-method='delete'>Delete</a>
					</td>
				</tr>
<%		}	%>
			</tbody>
		</table>
<%	end  %>
